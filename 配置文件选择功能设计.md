# 数据同步系统 - 多配置文件选择功能设计文档

## 1. 需求概述

### 1.1 当前状态
- 系统支持单一的 `tables_config.json` 配置文件
- 运行 `main.py` 直接执行配置文件中的所有表同步

### 1.2 新需求
- 支持多个表配置文件（如 `tables_config_1.json`, `tables_config_2.json` 等）
- 每个配置文件有自定义的名称/描述
- 运行启动后，在终端列出所有可用的配置文件
- 支持键盘上下键选择配置文件
- 按 Enter 确认后，执行选中配置文件的同步任务

---

## 2. 功能设计

### 2.1 配置文件结构

#### 统一目录结构（推荐）
```
config/
├── db_config.json                    # 数据库连接配置（全局）
└── table_configs/                    # 表配置文件目录
    ├── default.json                   # 默认表配置
    ├── prod.json                     # 生产环境表配置
    ├── test.json                     # 测试环境表配置
    ├── users.json                    # 用户表同步配置
    └── orders.json                   # 订单表同步配置
```

#### 目录说明
- **db_config.json**：数据库连接配置（全局，所有配置共享）
- **table_configs/**：专门存放所有表配置文件的目录
  - 所有表配置文件都放在这个目录下
  - 文件命名灵活，建议使用有意义的名称
  - 支持嵌套子目录进行分类（可选）

### 2.2 配置文件格式

#### 标准格式
```json
{
  "name": "生产环境全量同步",
  "description": "同步生产环境的所有核心表",
  "tables": [
    {
      "table_name": "BO_EU_FCST_PERIOD",
      "schema": "dbo",
      "sync_mode": "full",
      "sync_strategy": "overwrite",
      "primary_key": "id",
      "enabled": true,
      "sync_related_table": true,
      "source_database": "awscrmdb",
      "target_database": "aws"
    }
  ]
}
```

#### 字段说明
- **name**（必填）：配置文件的显示名称
- **description**（可选）：配置文件的详细描述
- **tables**：表配置列表（与原格式一致）

### 2.3 用户交互流程

```
============================================================
SQL Server 数据同步系统
============================================================

发现以下表配置文件:

  [●] 生产环境全量同步
      描述: 同步生产环境的所有核心表
      文件: prod.json

  [○] 测试环境增量同步
      描述: 测试环境的增量同步
      文件: test.json

  [○] 用户表同步
      描述: 仅同步用户相关表
      文件: users.json

请使用 ↑↓ 键选择配置文件，按 Enter 确认:
```

#### 交互说明
- `↑` 键：向上选择
- `↓` 键：向下选择
- `Enter` 键：确认选择，开始同步
- `Ctrl+C`：退出程序

---

## 3. 实现方案

### 3.1 新增文件

#### 3.1.1 配置选择器模块
**文件：** `src/config_selector.py`

**功能：**
```python
class ConfigSelector:
    """配置文件选择器"""
    
    def __init__(self, configs_dir: str = "config/table_configs"):
        """初始化配置选择器
        
        Args:
            configs_dir: 表配置文件目录
        """
        self.configs_dir = Path(configs_dir)
        self.configs = []
        self.current_index = 0
    
    def discover_configs(self) -> List[Dict[str, str]]:
        """发现所有表配置文件
        
        Returns:
            List[Dict]: 配置文件列表
            [{
                'filename': 'prod.json',
                'name': '生产环境全量同步',
                'description': '...',
                'path': Path('config/table_configs/prod.json')
            }]
        """
    
    def display_menu(self) -> None:
        """显示选择菜单"""
        # 清屏
        # 显示所有配置文件
        # 高亮显示当前选择
    
    def handle_input(self) -> Optional[str]:
        """处理用户输入
        
        Returns:
            str: 选中的配置文件路径，如果退出则返回None
        """
        # 监听键盘输入
        # ↑↓ 移动选择
        # Enter 确认
```

#### 3.1.2 主程序修改
**文件：** `main.py`

**修改内容：**
```python
from src.config_selector import ConfigSelector

def main():
    # 初始化配置选择器
    selector = ConfigSelector()
    
    # 发现配置文件
    configs = selector.discover_configs()
    
    if not configs:
        print("未找到任何表配置文件")
        print("请在 config/table_configs 目录下创建配置文件")
        sys.exit(1)
    
    # 显示选择菜单
    selected_config = selector.display_and_select(configs)
    
    if selected_config is None:
        print("用户取消操作")
        sys.exit(0)
    
    print(f"\n已选择配置: {selected_config['name']}")
    print(f"配置文件: {selected_config['filename']}")
    
    # 加载选中的配置
    config_loader = ConfigLoader(
        tables_config_path=selected_config['path']
    )
    
    # 继续原有的同步逻辑...
```

### 3.2 配置文件示例

#### 示例1：生产环境配置
**文件：** `config/table_configs/prod.json`
```json
{
  "name": "生产环境全量同步",
  "description": "同步生产环境的核心业务表，每天执行一次",
  "tables": [
    {
      "table_name": "BO_EU_FCST_PERIOD",
      "schema": "dbo",
      "sync_mode": "full",
      "sync_strategy": "overwrite",
      "primary_key": "id",
      "enabled": true,
      "sync_related_table": true,
      "source_database": "awscrmdb",
      "target_database": "aws"
    },
    {
      "table_name": "USER_INFO",
      "schema": "dbo",
      "sync_mode": "full",
      "sync_strategy": "overwrite",
      "primary_key": "id",
      "enabled": true,
      "sync_related_table": true,
      "source_database": "awscrmdb",
      "target_database": "aws"
    }
  ]
}
```

#### 示例2：测试环境配置
**文件：** `config/table_configs/test.json`
```json
{
  "name": "测试环境增量同步",
  "description": "测试环境的增量同步，每小时执行一次",
  "tables": [
    {
      "table_name": "ORDERS",
      "schema": "dbo",
      "sync_mode": "incremental",
      "sync_strategy": "append",
      "primary_key": "id",
      "last_sync_field": "update_time",
      "enabled": true,
      "sync_related_table": false,
      "source_database": "testdb",
      "target_database": "aws"
    }
  ]
}
```

#### 示例3：用户表专用配置
**文件：** `config/table_configs/users.json`
```json
{
  "name": "用户表同步",
  "description": "仅同步用户相关的表和关联数据",
  "tables": [
    {
      "table_name": "USER_INFO",
      "schema": "dbo",
      "sync_mode": "full",
      "sync_strategy": "overwrite",
      "primary_key": "id",
      "enabled": true,
      "sync_related_table": true,
      "source_database": "awscrmdb",
      "target_database": "aws"
    },
    {
      "table_name": "USER_PROFILES",
      "schema": "dbo",
      "sync_mode": "full",
      "sync_strategy": "overwrite",
      "primary_key": "id",
      "enabled": true,
      "sync_related_table": false,
      "source_database": "awscrmdb",
      "target_database": "aws"
    }
  ]
}
```

---

## 4. 配置文件管理

### 4.1 配置文件命名规范

#### 推荐命名模式
- `default.json` - 默认配置
- `<环境>.json` - 环境区分（prod, test, dev）
- `<功能>.json` - 功能区分（users, orders, products）
- `<日期>.json` - 版本区分（20251225, backup）

#### 有效文件名匹配规则
```python
# 匹配 table_configs/ 目录下的所有 .json 文件
- *.json
```

### 4.2 配置文件优先级

1. 如果只有一个配置文件 → 自动选择
2. 如果有多个配置文件 → 显示选择菜单
3. 如果 `default.json` 存在且是默认 → 可快速选择（快捷键 'd'）

---

## 5. 技术实现细节

### 5.1 键盘输入处理

使用 `keyboard` 库实现键盘事件监听：

```python
import keyboard

def wait_for_selection():
    """等待用户选择"""
    while True:
        if keyboard.is_pressed('up'):
            # 向上移动
            move_up()
        elif keyboard.is_pressed('down'):
            # 向下移动
            move_down()
        elif keyboard.is_pressed('enter'):
            # 确认选择
            return current_selection
        elif keyboard.is_pressed('esc') or keyboard.is_pressed('ctrl+c'):
            # 退出
            return None
```

### 5.2 终端显示优化

使用 ANSI 转义序列实现：
- 清屏：`\033[2J`
- 移动光标：`\033[H`
- 高亮显示：`\033[7m`
- 颜色显示：`\033[31m`（红色）、`\033[32m`（绿色）等

### 5.3 依赖项更新

**文件：** `requirements.txt`

新增依赖：
```
keyboard>=0.13.5
```

---

## 6. 使用场景

### 6.1 场景1：首次部署
```
1. 创建多个配置文件
2. 运行 python main.py
3. 选择 "生产环境全量同步"
4. 执行同步
```

### 6.2 场景2：定期执行
```
# 每天凌晨执行
0 2 * * * python main.py
→ 自动选择配置或脚本指定配置文件
```

### 6.3 场景3：快速测试
```
# 测试单个表
1. 创建 test.json
2. 运行 python main.py
3. 选择 "测试环境增量同步"
4. 验证结果
```

---

## 7. 向后兼容性

### 7.1 保持原有功能
- 单配置文件模式仍然支持
- 如果只有一个配置文件，自动选择
- 不破坏现有的自动化脚本

### 7.2 命令行参数支持（可选）
```bash
# 指定配置文件（跳过选择菜单）
python main.py --config prod.json

# 快捷模式（使用默认配置）
python main.py --default

# 列出所有配置
python main.py --list
```

---

## 8. 目录结构（最终）

```
data_sync/
├── venv/                          # 虚拟环境
├── config/                         # 配置文件目录
│   ├── db_config.json               # 数据库连接配置
│   ├── tables_config.json           # 旧版表配置（向后兼容）
│   └── table_configs/             # 表配置文件目录（新版）
│       ├── default.json           # 默认表配置
│       ├── prod.json             # 生产环境配置
│       ├── test.json             # 测试环境配置
│       └── users.json            # 用户表配置
├── src/                          # 源代码目录
│   ├── __init__.py
│   ├── config_loader.py          # 配置加载器
│   ├── config_selector.py        # 配置选择器（新增）
│   ├── db_connector.py           # 数据库连接器
│   ├── data_sync.py             # 数据同步核心
│   └── logger.py                # 日志模块
├── logs/                         # 日志文件目录
├── main.py                       # 主程序入口
├── requirements.txt              # Python依赖包
└── README.md                    # 项目说明文档
```

---

## 9. 开发步骤

### 阶段1：配置选择器开发
- [ ] 创建 `src/config_selector.py`
- [ ] 实现配置文件发现功能
- [ ] 实现菜单显示功能
- [ ] 实现键盘输入处理

### 阶段2：主程序集成
- [ ] 修改 `main.py` 集成配置选择器
- [ ] 实现配置加载逻辑
- [ ] 保持向后兼容性

### 阶段3：配置文件创建
- [ ] 创建 `config/table_configs` 目录
- [ ] 创建示例配置文件
- [ ] 编写使用文档

### 阶段4：测试和优化
- [ ] 测试单配置文件模式
- [ ] 测试多配置文件选择
- [ ] 测试键盘交互
- [ ] 优化用户体验

---

## 10. 注意事项

### 10.1 键盘兼容性
- Windows：`keyboard` 库兼容性良好
- Linux/macOS：可能需要额外权限
- 备选方案：使用 `input()` 数字选择

### 10.2 配置文件验证
- 检查必需字段（name, tables）
- 验证 JSON 格式
- 验证表配置有效性

### 10.3 错误处理
- 配置文件损坏时友好提示
- 键盘库缺失时降级处理
- 用户取消时优雅退出

---

## 11. 总结

本设计文档详细说明了多配置文件选择功能的实现方案，包括：

✅ **配置文件结构** - 清晰的目录结构和命名规范
✅ **用户交互** - 直观的菜单选择界面
✅ **技术实现** - 具体的代码实现方案
✅ **向后兼容** - 不破坏现有功能
✅ **使用场景** - 涵盖各种使用情况
✅ **开发步骤** - 明确的开发计划

该方案将所有表配置文件统一存放在 `config/table_configs/` 目录中，实现了灵活的多配置文件管理和用户友好的选择界面。
