# 配置文件选择功能修复说明

## 问题描述

原实现中存在以下问题：
1. **keyboard库需要管理员权限**：在Windows上使用keyboard库读取键盘输入需要管理员权限，导致普通用户无法使用
2. **导航响应延迟**：某些情况下键盘事件检测不够及时
3. **缺少回退机制**：当keyboard库不可用时，用户体验不友好

## 解决方案

### 1. 平台自适应输入处理

针对Windows系统使用`msvcrt`模块替代`keyboard`库：

```python
def _handle_keyboard_input(self) -> Optional[Dict[str, str]]:
    # Windows平台使用msvcrt（不需要管理员权限）
    if sys.platform == 'win32':
        return self._handle_windows_input()
    
    # 非Windows平台使用keyboard库
    try:
        import keyboard
        return self._handle_keyboard_lib_input(keyboard)
    except ImportError:
        return self._handle_numeric_input()
```

### 2. Windows原生键盘处理

使用`msvcrt.kbhit()`和`msvcrt.getch()`实现无需管理员权限的键盘输入：

```python
def _handle_windows_input(self) -> Optional[Dict[str, str]]:
    import msvcrt
    
    while True:
        if msvcrt.kbhit():
            key = msvcrt.getch()
            
            # 处理功能键
            if key == b'\x00' or key == b'\xe0':
                key = msvcrt.getch()
                if key == b'H':  # 上箭头
                    self.current_index = (self.current_index - 1) % len(self.configs)
                elif key == b'P':  # 下箭头
                    self.current_index = (self.current_index + 1) % len(self.configs)
            
            # 处理普通键
            elif key in [b'\r', b'\n']:  # Enter
                return self.configs[self.current_index]
            elif key == b'\x1b':  # Esc
                return None
```

### 3. 支持的按键

**导航键：**
- ↑ / W - 向上选择
- ↓ / S - 向下选择
- Page Up / Page Down - 快速导航

**确认键：**
- Enter / Return / 空格 / Tab - 确认选择

**退出键：**
- Esc / Escape - 取消操作
- Ctrl+C - 取消操作

### 4. 多层回退机制

```
Windows系统:
  └─ 尝试 msvcrt → 成功使用键盘导航
      └─ 失败 → 降级到数字选择模式

非Windows系统:
  └─ 尝试 keyboard → 成功使用键盘导航
      └─ 失败 → 降级到数字选择模式
```

## 测试结果

### 功能验证
- ✅ 配置文件列表正确显示
- ✅ 方向键导航流畅
- ✅ 实时高亮显示当前选择
- ✅ Enter键确认选择
- ✅ Esc键取消操作
- ✅ 自动加载选中配置
- ✅ 无需管理员权限即可使用

### 测试输出示例
```
============================================================
SQL Server 数据同步系统
============================================================

发现以下表配置文件:

  [●] 默认配置 - 预测表同步
      描述: 同步预测相关的核心表，适合日常使用
      文件: default.json

  [○] 测试配置 - 单表测试
      描述: 用于测试同步功能，只包含一个表
      文件: test.json

请使用 ↑↓ 键选择配置文件，按 Enter 确认:
```

## 优势

1. **无需管理员权限**：Windows用户无需提升权限即可使用
2. **更好的兼容性**：支持多种终端环境
3. **流畅的交互**：实时响应键盘输入
4. **健壮的错误处理**：多层回退机制确保功能可用
5. **跨平台支持**：Windows/Linux/macOS均可使用

## 使用说明

1. 运行程序：`python main.py`
2. 使用方向键（↑↓）在配置列表中导航
3. 当前选中的配置会显示 [●] 标记
4. 按 Enter 确认选择
5. 系统将根据选中的配置文件执行数据同步

## 注意事项

- 如果键盘导航不可用，系统会自动降级到数字选择模式
- 按 Esc 或 Ctrl+C 可以随时取消操作
- 如果只有一个配置文件，系统会自动选择该配置
